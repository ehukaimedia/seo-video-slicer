# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Python (backend) ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
.eslintcache

# --- Wheel build: UI + kernel bundled into the package by scripts/build-wheel.sh ---
backend/app/_web/
backend/app/_kernel/

# --- Runtime data: uploaded videos, preview/slice frames, working jobs ---
data/
.runtime/
.tmp-*/

# --- Local render inputs (e.g. Remotion .mp4s used to exercise the slicer) ---
videos/

# --- Scratch examples: generated test inputs/outputs, not source ---
# Ignore the scratch examples but KEEP the runnable Remotion example (source).
examples/*
!examples/remotion/

# --- Exported package zips ---
*.zip

# --- Premium-erase model cache (IOPaint / LaMa weights) ---
.iopaint/
models/lama/
*.ckpt
*.pt
*.pth

# --- Local agent / editor config (design system lives in DESIGN.md) ---
.claude/

# --- Secrets / local config ---
.env
.env.local
*.local

# --- Agent scratch context ---
.agents/
