# SEO Video Slicer — backend runtime dependencies (default / baseline install).
#
# This is the LEAN install: no torch, no neural model weights. The baseline erase
# tier (OpenCV INPAINT_NS), crop, WebP encode, ffmpeg/ffprobe, and the packager all
# work with just these. The premium erase tier (LaMa/IOPaint) is opt-in via
# requirements-premium.txt and is NEVER auto-installed.
#
# cv2 (opencv) and Pillow are inherited from the system site-packages on this
# machine (cv2 4.12, Pillow 11.3) and are intentionally NOT pinned here so we don't
# force a heavyweight wheel build on every user. If your environment lacks them,
# uncomment the two "optional" lines at the bottom.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
numpy>=1.24

# --- Optional (only if cv2 / Pillow are not already importable system-wide) ---
# opencv-python>=4.8
# pillow>=10.0
